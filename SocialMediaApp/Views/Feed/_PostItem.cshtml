@model SocialMediaApp.Models.ViewModels.PostViewModel

<div class="post-card mb-4 p-3 border rounded shadow-sm bg-white">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center">

        <div>
            <strong>@Model.Title</strong>
            <div class="text-muted small">
                by @Model.UserName
            </div>
        </div>

        <div class="text-muted small">
            @Model.CreatedAt.ToLocalTime().ToString("MMM dd, yyyy • HH:mm")
        </div>

    </div>

    <!-- Content -->
    <div class="mt-3">
        <p>@Model.Content</p>
    </div>

    <!-- Footer -->
    <div class="d-flex gap-4 text-muted mt-3">

        <span>
            👍 @Model.LikesCount
        </span>

        <span>
            💬 @Model.CommentsCount
        </span>

    </div>

</div>
